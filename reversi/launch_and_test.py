import shlex
from subprocess import Popen, PIPE

cmd = "pi/home/Documents/led-go/LED_Go_Board/reversi/Menu_main"
launchAgain = True
count = 0

while launchAgain and count < 100
	count++
	return_code = subprocess.call(cmd)
	if return_code == 0:
		launchAgain = False